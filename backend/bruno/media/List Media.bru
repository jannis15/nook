meta {
  name: List Media
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/media?limit=50
  body: none
  auth: inherit
}

script:post-response {
  bru.setEnvVar('mediaCursor', res.body.next_cursor ?? '');
}
